services:
  backend:
    build:
      context: .
      dockerfile: backend/Dockerfile
    container_name: pcap_backend
    ports:
      - "8000:8000"
    volumes:
      - ./backend:/app/backend
      - ./shared_data:/data